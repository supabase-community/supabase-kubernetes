apiVersion: core.supabase.io/v1alpha1
kind: SingleDatabase
metadata:
  name: supabase
spec:
  # pod:
  #   metadata:
  #     labels:
  #       workload: database
  #   spec:
  #     nodeSelector:
  #       workload: database
  storage:
    accessModes:
      - ReadWriteOnce
    size: 20Gi
    # deletionPolicy: Delete ## Or Retain
