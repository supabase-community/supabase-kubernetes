---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: projects.core.supabase.io
spec:
  group: core.supabase.io
  names:
    kind: Project
    listKind: ProjectList
    plural: projects
    singular: project
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Project is the Schema for the projects API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProjectSpec defines the desired state of a Supabase deployment.
            properties:
              auth:
                description: Auth defines the Auth component configuration
                properties:
                  additionalRedirectUrls:
                    description: AdditionalRedirectURLs is a list of additional URLs
                      allowed for redirects
                    items:
                      type: string
                    type: array
                  disableSignup:
                    default: false
                    description: DisableSignup disables new user signups
                    type: boolean
                  enable:
                    default: true
                    description: Enable defines whether the Auth component is enabled
                    type: boolean
                  enableAnonymousUsers:
                    default: false
                    description: EnableAnonymousUsers enables anonymous user signings
                    type: boolean
                  enableEmailAutoconfirm:
                    default: false
                    description: EnableEmailAutoconfirm skips email confirmation
                    type: boolean
                  enableEmailSignup:
                    default: true
                    description: EnableEmailSignup enables email/password signups
                    type: boolean
                  enableMailerSecureEmailChange:
                    description: EnableMailerSecureEmailChange enables secure email
                      change flow
                    type: boolean
                  enablePhoneAutoconfirm:
                    default: true
                    description: EnablePhoneAutoconfirm skips phone confirmation
                    type: boolean
                  enablePhoneSignup:
                    default: true
                    description: EnablePhoneSignup enables phone signups
                    type: boolean
                  mfa:
                    description: MFA defines multi-factor authentication configuration
                    properties:
                      enablePhoneEnroll:
                        description: EnablePhoneEnroll defines whether phone MFA enrollment
                          is enabled
                        type: boolean
                      enablePhoneVerify:
                        description: EnablePhoneVerify defines whether phone MFA verification
                          is enabled
                        type: boolean
                      enableTotpEnroll:
                        description: EnableTOTPEnroll defines whether TOTP enrollment
                          is enabled
                        type: boolean
                      enableTotpVerify:
                        description: EnableTOTPVerify defines whether TOTP verification
                          is enabled
                        type: boolean
                      maxEnrolledFactors:
                        description: MaxEnrolledFactors defines the maximum number
                          of enrolled MFA factors
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                  oauth:
                    description: OAuth defines OAuth provider configuration
                    properties:
                      azure:
                        description: Azure defines the Azure OAuth provider configuration
                        properties:
                          clientId:
                            description: ClientID defines the OAuth client ID
                            minLength: 1
                            type: string
                          enable:
                            default: true
                            description: Enable defines whether the OAuth provider
                              is enabled
                            type: boolean
                          secretRef:
                            description: SecretRef references the secret containing
                              the OAuth client secret
                            properties:
                              key:
                                description: Key defines the key within the Kubernetes
                                  Secret
                                maxLength: 253
                                minLength: 1
                                type: string
                              name:
                                description: Name defines the name of the Kubernetes
                                  Secret
                                maxLength: 253
                                minLength: 1
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          url:
                            description: |-
                              URL overrides the OAuth provider base URL. Required for Keycloak
                              (realm URL, e.g. https://keycloak.example.com/realms/myrealm)
                            type: string
                        required:
                        - clientId
                        - secretRef
                        type: object
                      github:
                        description: GitHub defines the GitHub OAuth provider configuration
                        properties:
                          clientId:
                            description: ClientID defines the OAuth client ID
                            minLength: 1
                            type: string
                          enable:
                            default: true
                            description: Enable defines whether the OAuth provider
                              is enabled
                            type: boolean
                          secretRef:
                            description: SecretRef references the secret containing
                              the OAuth client secret
                            properties:
                              key:
                                description: Key defines the key within the Kubernetes
                                  Secret
                                maxLength: 253
                                minLength: 1
                                type: string
                              name:
                                description: Name defines the name of the Kubernetes
                                  Secret
                                maxLength: 253
                                minLength: 1
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          url:
                            description: |-
                              URL overrides the OAuth provider base URL. Required for Keycloak
                              (realm URL, e.g. https://keycloak.example.com/realms/myrealm)
                            type: string
                        required:
                        - clientId
                        - secretRef
                        type: object
                      google:
                        description: Google defines the Google OAuth provider configuration
                        properties:
                          clientId:
                            description: ClientID defines the OAuth client ID
                            minLength: 1
                            type: string
                          enable:
                            default: true
                            description: Enable defines whether the OAuth provider
                              is enabled
                            type: boolean
                          secretRef:
                            description: SecretRef references the secret containing
                              the OAuth client secret
                            properties:
                              key:
                                description: Key defines the key within the Kubernetes
                                  Secret
                                maxLength: 253
                                minLength: 1
                                type: string
                              name:
                                description: Name defines the name of the Kubernetes
                                  Secret
                                maxLength: 253
                                minLength: 1
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          url:
                            description: |-
                              URL overrides the OAuth provider base URL. Required for Keycloak
                              (realm URL, e.g. https://keycloak.example.com/realms/myrealm)
                            type: string
                        required:
                        - clientId
                        - secretRef
                        type: object
                      keycloak:
                        description: Keycloak defines the Keycloak OAuth provider
                          configuration
                        properties:
                          clientId:
                            description: ClientID defines the OAuth client ID
                            minLength: 1
                            type: string
                          enable:
                            default: true
                            description: Enable defines whether the OAuth provider
                              is enabled
                            type: boolean
                          secretRef:
                            description: SecretRef references the secret containing
                              the OAuth client secret
                            properties:
                              key:
                                description: Key defines the key within the Kubernetes
                                  Secret
                                maxLength: 253
                                minLength: 1
                                type: string
                              name:
                                description: Name defines the name of the Kubernetes
                                  Secret
                                maxLength: 253
                                minLength: 1
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          url:
                            description: |-
                              URL overrides the OAuth provider base URL. Required for Keycloak
                              (realm URL, e.g. https://keycloak.example.com/realms/myrealm)
                            type: string
                        required:
                        - clientId
                        - secretRef
                        type: object
                    type: object
                  pod:
                    description: Pod overlays the operator-generated Pod template.
                    x-kubernetes-preserve-unknown-fields: true
                  replicas:
                    default: 1
                    description: Replicas defines the number of component instances
                    format: int32
                    minimum: 0
                    type: integer
                  saml:
                    description: SAML defines SAML authentication configuration
                    properties:
                      allowEncryptedAssertions:
                        description: AllowEncryptedAssertions defines whether encrypted
                          SAML assertions are allowed
                        type: boolean
                      enable:
                        default: true
                        description: Enable defines whether SAML authentication is
                          enabled
                        type: boolean
                      rateLimitAssertion:
                        description: RateLimitAssertion defines the rate limit for
                          SAML assertions
                        format: int32
                        minimum: 1
                        type: integer
                      relayStateValidityPeriod:
                        description: RelayStateValidityPeriod defines the validity
                          period for the relay state
                        type: string
                    type: object
                  service:
                    description: Service defines the configuration for the component
                      Service
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations defines annotations to add to the
                          Service
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels defines labels to add to the Service
                        type: object
                      type:
                        default: ClusterIP
                        description: Type defines the type of the Kubernetes Service
                        enum:
                        - ClusterIP
                        - NodePort
                        - LoadBalancer
                        type: string
                    type: object
                  siteUrl:
                    description: SiteURL is the base URL of the site used for email
                      links and redirects
                    minLength: 1
                    type: string
                  skipNonceCheck:
                    description: SkipNonceCheck skips nonce check for external providers
                    type: boolean
                  sms:
                    description: SMS defines SMS provider configuration
                    properties:
                      maxFrequency:
                        description: MaxFrequency defines the maximum frequency for
                          sending SMS messages
                        minLength: 1
                        type: string
                      otpExp:
                        description: OTPExp defines the OTP expiration time in seconds
                        format: int32
                        minimum: 1
                        type: integer
                      otpLength:
                        description: OTPLength defines the length of the OTP
                        format: int32
                        maximum: 10
                        minimum: 4
                        type: integer
                      provider:
                        description: Provider defines the SMS provider name
                        minLength: 1
                        type: string
                      template:
                        description: Template defines the SMS message template
                        minLength: 1
                        type: string
                      twilio:
                        description: Twilio defines the Twilio provider configuration
                        properties:
                          accountSid:
                            description: AccountSID defines the Twilio account SID
                            minLength: 1
                            type: string
                          authTokenRef:
                            description: AuthTokenRef references the secret containing
                              the Twilio auth token
                            properties:
                              key:
                                description: Key defines the key within the Kubernetes
                                  Secret
                                maxLength: 253
                                minLength: 1
                                type: string
                              name:
                                description: Name defines the name of the Kubernetes
                                  Secret
                                maxLength: 253
                                minLength: 1
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          messageServiceSid:
                            description: MessageServiceSID defines the Twilio message
                              service SID
                            minLength: 1
                            type: string
                        required:
                        - accountSid
                        - authTokenRef
                        - messageServiceSid
                        type: object
                    required:
                    - maxFrequency
                    - otpExp
                    - otpLength
                    - provider
                    - template
                    type: object
                  smtp:
                    description: SMTP defines SMTP configuration for sending emails
                    properties:
                      adminEmail:
                        description: AdminEmail defines the admin email address
                        minLength: 1
                        type: string
                      host:
                        description: Host defines the SMTP server host
                        minLength: 1
                        type: string
                      maxFrequency:
                        description: MaxFrequency defines the maximum frequency for
                          sending emails
                        type: string
                      passwordRef:
                        description: PasswordRef references the secret containing
                          the SMTP password
                        properties:
                          key:
                            description: Key defines the key within the Kubernetes
                              Secret
                            maxLength: 253
                            minLength: 1
                            type: string
                          name:
                            description: Name defines the name of the Kubernetes Secret
                            maxLength: 253
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      port:
                        description: Port defines the SMTP server port
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      senderName:
                        description: SenderName defines the display name for outgoing
                          emails
                        minLength: 1
                        type: string
                      user:
                        description: User defines the SMTP authentication user
                        minLength: 1
                        type: string
                    required:
                    - adminEmail
                    - host
                    - passwordRef
                    - port
                    - senderName
                    - user
                    type: object
                required:
                - siteUrl
                type: object
              databaseRef:
                description: DatabaseRef references the database resource
                properties:
                  kind:
                    description: Kind defines the kind of database resource
                    enum:
                    - SingleDatabase
                    minLength: 1
                    type: string
                  name:
                    description: Name defines the name of the database resource
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - kind
                - name
                type: object
              envoy:
                description: Envoy defines the Envoy component configuration
                properties:
                  enable:
                    default: true
                    description: Enable defines whether the Envoy component is enabled
                    type: boolean
                  pod:
                    description: Pod overlays the operator-generated Pod template.
                    x-kubernetes-preserve-unknown-fields: true
                  replicas:
                    default: 1
                    description: Replicas defines the number of component instances
                    format: int32
                    minimum: 0
                    type: integer
                  service:
                    description: Service defines the configuration for the component
                      Service
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations defines annotations to add to the
                          Service
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels defines labels to add to the Service
                        type: object
                      type:
                        default: ClusterIP
                        description: Type defines the type of the Kubernetes Service
                        enum:
                        - ClusterIP
                        - NodePort
                        - LoadBalancer
                        type: string
                    type: object
                type: object
              functions:
                description: Functions defines the Functions component configuration
                properties:
                  enable:
                    default: true
                    description: Enable defines whether the Functions component is
                      enabled
                    type: boolean
                  pod:
                    description: Pod overlays the operator-generated Pod template.
                    x-kubernetes-preserve-unknown-fields: true
                  replicas:
                    default: 1
                    description: Replicas defines the number of component instances
                    format: int32
                    minimum: 0
                    type: integer
                  service:
                    description: Service defines the configuration for the component
                      Service
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations defines annotations to add to the
                          Service
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels defines labels to add to the Service
                        type: object
                      type:
                        default: ClusterIP
                        description: Type defines the type of the Kubernetes Service
                        enum:
                        - ClusterIP
                        - NodePort
                        - LoadBalancer
                        type: string
                    type: object
                  verifyJwt:
                    description: VerifyJWT defines whether to verify JWT tokens
                    type: boolean
                required:
                - verifyJwt
                type: object
              http:
                description: HTTP defines the public HTTP access settings
                properties:
                  hostname:
                    description: Hostname defines the public hostname for the Project
                    minLength: 1
                    type: string
                  port:
                    description: Port defines the public port for the Project
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  protocol:
                    description: Protocol defines the HTTP protocol (http or https)
                    enum:
                    - http
                    - https
                    type: string
                required:
                - hostname
                - protocol
                type: object
              jwtExpSec:
                default: 3600
                description: JWTExpSec defines the JWT expiration time in seconds
                format: int32
                minimum: 1
                type: integer
              meta:
                description: Meta defines the Meta component configuration
                properties:
                  enable:
                    default: true
                    description: Enable defines whether the Meta component is enabled
                    type: boolean
                  pod:
                    description: Pod overlays the operator-generated Pod template.
                    x-kubernetes-preserve-unknown-fields: true
                  replicas:
                    default: 1
                    description: Replicas defines the number of component instances
                    format: int32
                    minimum: 0
                    type: integer
                  service:
                    description: Service defines the configuration for the component
                      Service
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations defines annotations to add to the
                          Service
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels defines labels to add to the Service
                        type: object
                      type:
                        default: ClusterIP
                        description: Type defines the type of the Kubernetes Service
                        enum:
                        - ClusterIP
                        - NodePort
                        - LoadBalancer
                        type: string
                    type: object
                type: object
              realtime:
                description: Realtime defines the Realtime component configuration
                properties:
                  enable:
                    default: true
                    description: Enable defines whether the Realtime component is
                      enabled
                    type: boolean
                  pod:
                    description: Pod overlays the operator-generated Pod template.
                    x-kubernetes-preserve-unknown-fields: true
                  replicas:
                    default: 1
                    description: Replicas defines the number of component instances
                    format: int32
                    minimum: 0
                    type: integer
                  service:
                    description: Service defines the configuration for the component
                      Service
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations defines annotations to add to the
                          Service
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels defines labels to add to the Service
                        type: object
                      type:
                        default: ClusterIP
                        description: Type defines the type of the Kubernetes Service
                        enum:
                        - ClusterIP
                        - NodePort
                        - LoadBalancer
                        type: string
                    type: object
                type: object
              rest:
                description: Rest defines the Rest component configuration
                properties:
                  dbExtraSearchPath:
                    default: public
                    description: DBExtraSearchPath defines the schemas to add to the
                      search path of every request
                    type: string
                  dbMaxRows:
                    default: 1000
                    description: DBMaxRows defines the maximum number of rows returned
                      from a view, table, or stored procedure
                    format: int32
                    minimum: 1
                    type: integer
                  dbSchemas:
                    default: public,storage,graphql_public
                    description: DBSchemas defines the schemas exposed by PostgREST
                    type: string
                  enable:
                    default: true
                    description: Enable defines whether the Rest component is enabled
                    type: boolean
                  pod:
                    description: Pod overlays the operator-generated Pod template.
                    x-kubernetes-preserve-unknown-fields: true
                  replicas:
                    default: 1
                    description: Replicas defines the number of component instances
                    format: int32
                    minimum: 0
                    type: integer
                  service:
                    description: Service defines the configuration for the component
                      Service
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations defines annotations to add to the
                          Service
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels defines labels to add to the Service
                        type: object
                      type:
                        default: ClusterIP
                        description: Type defines the type of the Kubernetes Service
                        enum:
                        - ClusterIP
                        - NodePort
                        - LoadBalancer
                        type: string
                    type: object
                type: object
              storage:
                description: Storage defines the Storage component configuration
                properties:
                  enable:
                    default: true
                    description: Enable defines whether the Storage component is enabled
                    type: boolean
                  fileSizeLimit:
                    default: 52428800
                    description: FileSizeLimit defines the maximum file size in bytes
                    format: int64
                    minimum: 1
                    type: integer
                  pod:
                    description: Pod overlays the operator-generated Pod template.
                    x-kubernetes-preserve-unknown-fields: true
                  replicas:
                    default: 1
                    description: Replicas defines the number of Storage instances
                    format: int32
                    minimum: 0
                    type: integer
                  service:
                    description: Service defines the configuration for the component
                      Service
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations defines annotations to add to the
                          Service
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels defines labels to add to the Service
                        type: object
                      type:
                        default: ClusterIP
                        description: Type defines the type of the Kubernetes Service
                        enum:
                        - ClusterIP
                        - NodePort
                        - LoadBalancer
                        type: string
                    type: object
                  storage:
                    description: Storage defines the persistent volume claim for Storage
                      data
                    properties:
                      accessModes:
                        description: AccessModes defines the access modes for the
                          persistent volume claim
                        items:
                          type: string
                        type: array
                      deletionPolicy:
                        allOf:
                        - enum:
                          - Delete
                          - Retain
                        - enum:
                          - Delete
                          - Retain
                        default: Delete
                        description: DeletionPolicy defines the deletion behavior
                          for the persistent volume claim
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Size defines the size of the persistent volume
                          claim
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: StorageClassName defines the storage class name
                          for the persistent volume claim
                        type: string
                    required:
                    - accessModes
                    - size
                    type: object
                required:
                - storage
                type: object
              studio:
                description: Studio defines the Studio component configuration
                properties:
                  enable:
                    default: true
                    description: Enable defines whether the Studio component is enabled
                    type: boolean
                  openAiApiKey:
                    description: OpenAIAPIKey references the secret containing the
                      OpenAI API key
                    properties:
                      key:
                        description: Key defines the key within the Kubernetes Secret
                        maxLength: 253
                        minLength: 1
                        type: string
                      name:
                        description: Name defines the name of the Kubernetes Secret
                        maxLength: 253
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  orgName:
                    description: OrgName defines the default organization name shown
                      in Studio
                    minLength: 1
                    type: string
                  pod:
                    description: Pod overlays the operator-generated Pod template.
                    x-kubernetes-preserve-unknown-fields: true
                  projName:
                    description: ProjName defines the default project name shown in
                      Studio
                    minLength: 1
                    type: string
                  replicas:
                    default: 1
                    description: Replicas defines the number of Studio instances
                    format: int32
                    minimum: 0
                    type: integer
                  service:
                    description: Service defines the configuration for the component
                      Service
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations defines annotations to add to the
                          Service
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels defines labels to add to the Service
                        type: object
                      type:
                        default: ClusterIP
                        description: Type defines the type of the Kubernetes Service
                        enum:
                        - ClusterIP
                        - NodePort
                        - LoadBalancer
                        type: string
                    type: object
                  storage:
                    description: Storage defines the persistent volume claim for Studio
                      snippets
                    properties:
                      accessModes:
                        description: AccessModes defines the access modes for the
                          persistent volume claim
                        items:
                          type: string
                        type: array
                      deletionPolicy:
                        allOf:
                        - enum:
                          - Delete
                          - Retain
                        - enum:
                          - Delete
                          - Retain
                        default: Delete
                        description: DeletionPolicy defines the deletion behavior
                          for the persistent volume claim
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Size defines the size of the persistent volume
                          claim
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: StorageClassName defines the storage class name
                          for the persistent volume claim
                        type: string
                    required:
                    - accessModes
                    - size
                    type: object
                required:
                - orgName
                - projName
                - storage
                type: object
            required:
            - databaseRef
            - http
            type: object
          status:
            description: ProjectStatus defines the observed state of a Supabase deployment.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the Project's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              jwtSyncHash:
                description: JwtSyncHash is the hash of the JWT configuration last
                  successfully applied by the sync job.
                type: string
              passwordSyncHash:
                description: PasswordSyncHash is the hash of the password configuration
                  last successfully applied by the sync job.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
