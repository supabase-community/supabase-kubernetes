---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: singledatabases.core.supabase.io
spec:
  group: core.supabase.io
  names:
    kind: SingleDatabase
    listKind: SingleDatabaseList
    plural: singledatabases
    singular: singledatabase
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SingleDatabase is the Schema for the singledatabases API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SingleDatabaseSpec defines the desired state of SingleDatabase.
            properties:
              pod:
                description: Pod overlays the operator-generated Pod template.
                x-kubernetes-preserve-unknown-fields: true
              service:
                description: Service defines the configuration for the component Service
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations defines annotations to add to the Service
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels defines labels to add to the Service
                    type: object
                  type:
                    default: ClusterIP
                    description: Type defines the type of the Kubernetes Service
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    type: string
                type: object
              storage:
                description: Storage defines the persistent volume claim configuration
                properties:
                  accessModes:
                    description: AccessModes defines the access modes for the persistent
                      volume claim
                    items:
                      type: string
                    type: array
                  deletionPolicy:
                    allOf:
                    - enum:
                      - Delete
                      - Retain
                    - enum:
                      - Delete
                      - Retain
                    default: Delete
                    description: DeletionPolicy defines the deletion behavior for
                      the persistent volume claim
                    type: string
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    description: Size defines the size of the persistent volume claim
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    description: StorageClassName defines the storage class name for
                      the persistent volume claim
                    type: string
                required:
                - accessModes
                - size
                type: object
            required:
            - storage
            type: object
          status:
            description: SingleDatabaseStatus defines the observed state of SingleDatabase.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the SingleDatabase's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              resolvedDatabase:
                description: ResolvedDatabase exposes the resolved database connection
                  parameters
                properties:
                  dbName:
                    description: DBName defines the database name
                    type: string
                  host:
                    description: Host defines the database host
                    type: string
                  passwordRef:
                    description: PasswordRef references the secret containing the
                      database password
                    properties:
                      key:
                        description: Key defines the key within the Kubernetes Secret
                        maxLength: 253
                        minLength: 1
                        type: string
                      name:
                        description: Name defines the name of the Kubernetes Secret
                        maxLength: 253
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  port:
                    description: Port defines the database port
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  user:
                    description: User defines the database user
                    type: string
                required:
                - dbName
                - host
                - passwordRef
                - port
                - user
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
