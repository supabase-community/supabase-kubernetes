Supabase Project "{{ include "supabase-project.fullname" . }}" has been created.

To watch the status:
  kubectl get project {{ include "supabase-project.fullname" . }} -n {{ .Release.Namespace }} -w

To check the SingleDatabase status:
  kubectl get singledatabase {{ include "supabase-project.fullname" . }} -n {{ .Release.Namespace }} -w
