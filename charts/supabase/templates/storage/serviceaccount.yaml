{{- if .Values.storage.enabled -}}
{{- if .Values.serviceAccount.storage.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "supabase.storage.serviceAccountName" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.storage.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
