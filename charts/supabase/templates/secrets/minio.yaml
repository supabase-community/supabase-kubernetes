{{- if .Values.secret.minio }}
{{- if not .Values.secret.minio.secretRef }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "supabase.secret.minio" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
type: Opaque
data:
{{- range $key, $value := .Values.secret.minio }}
{{- if $value }}
{{- if eq (typeOf $value) "string" }}
  {{ $key }}: {{ $value | toString | b64enc }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
