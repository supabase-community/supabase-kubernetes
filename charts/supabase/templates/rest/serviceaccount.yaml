{{- if .Values.deployment.rest.enabled -}}
{{- if .Values.serviceAccount.rest.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "supabase.rest.serviceAccountName" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.rest.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
